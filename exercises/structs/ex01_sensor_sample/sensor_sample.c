#include <stdio.h>

/*
 * Struct exercise: model one sensor sample.
 *
 * You must type:
 * - the needed #include directives
 * - the enum definition
 * - the struct definition
 * - any function declarations
 * - any function definitions
 * - all parameter lists and return types
 * - all local variables
 * - a main function with tests
 *
 * Suggested names:
 * - enum sample_status
 * - struct sensor_sample
 * - print_sample
 *
 * Your test program should print results for:
 * - normal sample
 * - warning threshold
 * - fault threshold
 * - minimum raw count
 * - maximum raw count
 */

/* Add your code below this line. */
